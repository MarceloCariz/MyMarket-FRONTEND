import { ContainerCartList } from '@/components';
import { CenterColumn } from '@/styles';

const Cart = () => {
  return (
    <CenterColumn>
      <ContainerCartList />
    </CenterColumn>
  );
};

export default Cart;
